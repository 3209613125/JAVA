public class yg {
    public static void main(String[] args) {

        for(int s=0;s<10;s++){
            double a= (double) (Math.random()*3);
            System.out.println(a);
        }
    }
}
//默认值为基本数据类型的默认值，左闭（默认值），右开（* x）
